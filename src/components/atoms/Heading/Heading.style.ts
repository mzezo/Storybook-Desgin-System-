import styled from 'styled-components'
import {
    fontFamily,
    fontSize,
    fontWeight,
    color,
    textAlign,
    lineHeight,
    letterSpacing,
  } from 'styled-system'

export const HeadingWrapper = styled('p')(
    fontFamily,
    fontSize,
    color,
    fontWeight,
    textAlign,
    lineHeight,
    letterSpacing,
);


